# Week 14 讲义：业界架构演进与三巨头最新架构解析

> **本周主题**：Phase 7 启动。第一部分快过 2017-2024 的架构演进谱系（原理在 Week 1-2 已详讲）；主体部分深入解析 2026 年三巨头的最新架构——**DeepSeek V4/V4.1、Qwen3.8-Flash-Next、Kimi K3/K2.8**，收官阶段建立"厂商谱系"的全局视野。

---

## 🎯 核心目标

学完本周，你应该能够：

1. **演进视角**：能画出主流模型的设计选择对比表，理解演进背后的"两股绳"（能力 vs 效率）如何驱动每个决策
2. **DeepSeek**：理解 V4 如何把 1M 上下文变成日常可用（mHC 残差重构 + CSA/HCA 序列维压缩 + 训练栈工程），以及 V4.1 非对称编解码的原理
3. **Qwen**：理解 3.8-Flash-Next 的五层解耦设计，特别是 51B N-gram 显式查表记忆的工程意义
4. **Kimi**：理解 KDA 的逐维遗忘机制与 AttnRes 的深度维选择性检索，以及两者与 mHC 的哲学对比
5. **横向综合**：能总结 2026 年三巨头架构收敛的五大主题，并解释为什么"Transformer 标准组件"本身正在被重写

## 🗺️ 知识图谱

```
业界架构演进（快过）
    ├── 2017-2024 谱系收敛：GPT-3 → LLaMA → Llama-2/3 → Qwen/Mixtral
    ├── 两股绳：能力（深/宽/长） vs 效率（计算/显存/通信）
    └── 2025-2026 架构革命：五条战线同时开打
              ↓
三巨头深度解析
    ├── DeepSeek V4/V4.1（主线：1M 上下文日用化）
    │   ├── mHC：残差连接 → 流形约束超连接（深度维）
    │   ├── CSA+HCA：序列维混合压缩（KV 条数 n 的压缩）
    │   ├── 训练栈：Muon / FP4 QAT / Anticipatory Routing
    │   └── V4.1 Flash：非对称编解码（输入 8B / 输出 16B）
    ├── Qwen3.8-Flash-Next（主线：容量与计算五层解耦）
    │   ├── GDN+QSA 3:1 混合注意力（衔接 W8）
    │   ├── 51B N-gram Embedding：显式查表记忆（衔接 W9）
    │   └── Gated Residual + Muon：训练稳定性
    └── Kimi K3/K2.8（主线：信息流动的时间维与深度维）
        ├── KDA：标量遗忘 → 逐维对角遗忘（时间维）
        ├── AttnRes：固定残差 → 层间注意力（深度维）
        └── 896选16 超稀疏 MoE + LatentMoE/Quantile Balancing
              ↓
横向综合：收敛五大主题
    序列维压缩 / 深度维重构 / 超稀疏 MoE / 显式记忆回归 / 训练稳定性工程
```

**与前几周的衔接**：本周是"架构线"的终点站——Week 1-2 打的架构地基（RoPE/RMSNorm/SwiGLU/MoE）、Week 6 的推理工程（KV Cache/prefill-decode 不对称/量化）、Week 8 的多模态骨干（Qwen3.5-Omni 的 Hybrid MoE+GDN）、Week 9 的记忆系统、Week 13 的长上下文评测（RULER 有效长度），全部在本周汇合。阅读时请把这些锚点放在手边。

---

# Part 1: 业界架构演进概览（快过）

> **本部分策略**：设计原理（RMSNorm 为什么好、RoPE 怎么旋转、MoE 怎么路由）在 Week 1-2 已经详讲，此处不重复展开，只做两件事：(1) 一张表收拢谱系；(2) 提炼驱动演进的"两股绳"与 2025-2026 的革命格局，为三家解析做铺垫。

## 1.1 谱系收敛：2017-2024

| 模型 | 时间 | Norm | 激活 | 位置编码 | 注意力 | FFN | 关键贡献 |
|------|------|------|------|---------|--------|-----|---------|
| GPT-3 | 2020-06 | Post-LN | GELU | 可学习绝对 | MHA | Dense | 证明 Scaling Law 可行（见 W1） |
| LLaMA | 2023-02 | **Pre-RMSNorm** | **SwiGLU** | **RoPE** | MHA | Dense | 开源谱系的"标准件"起点（见 W1） |
| Llama-2 70B | 2023-07 | 同上 | SwiGLU | RoPE | **GQA** | Dense | GQA 进入主流（KV Cache 压缩 8x） |
| Mixtral 8x7B | 2023-12 | 同上 | SwiGLU | RoPE | GQA | **MoE** | 开源 MoE 旗手（见 W2） |
| Llama-3 | 2024-04 | 同上 | SwiGLU | RoPE | GQA | Dense | 8k→128k 长上下文工程化 |
| Qwen3 | 2025-04 | 同上 | SwiGLU | RoPE | GQA | **MoE** | Dense/MoE 双线 + 思考模式 |
| DeepSeek-V2/V3 | 2024-05/12 | 同上 | SwiGLU | 解耦 RoPE | **MLA** | **DeepSeekMoE** | MLA + Sigmoid 路由（见 W2） |

**读这张表的方法**：从 GPT-3 到 LLaMA 的三处替换（Pre-RMSNorm/SwiGLU/RoPE）不是"更好"这么简单——三者共同把**训练稳定性**和**外推性**提到工程可用的水平（Pre-LN 去掉 warmup 依赖，RoPE 支持长度外推，见 W1）。LLaMA 之后，开源界的"标准件"十年未变，变化都发生在注意力头组织（MHA→GQA→MLA）和 FFN 稀疏化（Dense→MoE）两条支线上。

## 1.2 演进的两股绳

回头看每一步替换，背后都是同一对张力的不同侧面：

**绳 1：能力**（把模型做得更强）
- 深：层数增加 → 表达力增强，但训练不稳定（残差流的信号控制问题）
- 宽：参数量增加 → 知识容量增加，但成本线性上升
- 长：上下文扩展 → 应用价值增加，但 $O(n^2)$ 计算与 KV Cache 线性膨胀

**绳 2：效率**（把能力做得便宜）
- 计算效率：MoE 激活解耦、线性注意力、稀疏注意力
- 显存效率：GQA/MLA 压 KV、量化（BF16→FP8→FP4，见 W6）
- 训练效率：Muon 优化器、FP8/FP4 训练、并行策略（见 W2）

**关键观察**：2024 年之前，绳 2 的创新大多发生在"组件级替换"（换个激活函数、换个注意力变体），Transformer 的骨架——**残差连接 + 注意力层 + FFN 层的堆叠范式**——从未被动摇。而 2025-2026 年的革命不同：**骨架本身被重写了**。

## 1.3 2025-2026：五条战线同时开打

这是本周主体部分的地图。三巨头的架构创新可以放进一张"五条战线"矩阵：

| 战线 | 旧范式 | 新方向 | 主要玩家 |
|------|--------|--------|---------|
| **注意力：序列维** | 全注意力 $O(n^2)$、KV 随 n 膨胀 | 线性注意力 + 稀疏检索混合（GDN/QSA、CSA/HCA、KDA） | 三家全部 |
| **残差：深度维** | 固定残差求和（ResNet, 2015） | 流形约束多路残差（mHC）、层间注意力（AttnRes）、门控残差（GR） | DeepSeek/Kimi/Qwen |
| **FFN：稀疏度** | Top-8 MoE（激活比 ~1/7） | 超稀疏 MoE（896选16，56:1）+ 负载均衡新解法 | Kimi/Qwen |
| **记忆：表示形态** | 全部参数化（隐式记忆） | 显式查表记忆回归（51B N-gram Embedding） | Qwen |
| **训练：稳定性** | AdamW + BF16 | Muon + FP8/FP4 QAT + 路由级稳定性工程 | DeepSeek/Qwen/Kimi |

**为什么是现在？** 三个外部条件同时成熟：

1. **1M 上下文成为产品标配**（Week 13 看到的评测侧需求倒逼架构侧）：1M token 下，标准注意力的 KV Cache 按旧范式根本放不下——RULER 揭示的"声称长度 vs 有效长度"裂缝，架构必须正面回应；
2. **模型深度跨过临界点**：百层以上的深层网络，固定残差求和的信号稀释/发散问题从"理论隐患"变成"工程现实"；
3. **推理成本成为竞争主战场**：Agent 时代（Phase 5）的 token 消耗量是 Chatbot 时代的百倍，效率架构直接决定商业模型。

带着这张地图，我们逐一进入三家。

---

# Part 2: DeepSeek V4/V4.1 深度解析

## 2.1 主线：把 1M 上下文变成日常可用

DeepSeek-V4 技术报告（2026-04-24）的核心叙事不是"又变强了"，而是一个工程命题：**要把 1M context 从"顶到 1M 的能力演示"变成"推理服务实际可承担的日常配置"**。这需要架构、infra、训练、后训练全链条协同，最终把 1M token 场景下的**推理 FLOPs 压到 V3.2 的 27%、KV Cache 压到 10%**。

V4 延续了 DeepSeekMoE + MTP（Multi-Token Prediction，多 token 预测投机解码模块）的骨架，但在三个关键位置做了替换：

| 位置 | V3 时代 | V4 时代 |
|------|---------|---------|
| 残差连接 | 标准残差 | **mHC（流形约束超连接）** |
| 注意力 | MLA | **CSA + HCA 混合注意力** |
| 优化器 | Adam（FP8 混合精度） | **Muon（部分替代）+ FP4 QAT** |

下面逐一展开。MLA 的原理本身在本课程已完整覆盖（数学推导、解耦 RoPE、矩阵吸收见 `专题学习/MLA专题讲义.md`，W2 亦有速览），本节聚焦"V4 为什么要动它"。

## 2.2 mHC：残差连接的重构

### 2.2.1 问题：残差连接的"恒等映射"红利正在耗尽

先回顾我们熟悉的世界（W1）：Transformer 的每一层做的是

$$h_{l+1} = h_l + F_l(h_l)$$

这个形式（ResNet, 2015）的伟大之处在于**恒等映射性质**：当 $F_l \to 0$ 时，网络自动退化为恒等函数，梯度可以通过"加法捷径"无损回传。这是深层网络可训练的根本原因——训练初期的网络接近恒等，信息流和梯度流都是"保真"的。

问题出在两个新需求上：

1. **HC 的尝试与代价**。Hyper-Connections（HC）把单条残差流扩展为多条（扩展宽度），并在层间做多样化的线性混合——性能确实涨了，但**多样化连接破坏了恒等映射性质**：信号在多层混合矩阵间传播时，范数可能无界放大或衰减，导致训练不稳定，且额外的混合矩阵带来内存访问开销。深层网络（100+ 层）下这个问题是致命的。
2. **V4 的深度**。1M 上下文的模型需要非常深的网络（时间维压缩靠层数换取），训练稳定性从"锦上添花"变成"能不能训出来"。

### 2.2.2 mHC 的核心思想：把残差映射约束到双随机矩阵流形

mHC（Manifold-Constrained Hyper-Connections，arXiv 2512.24880）的解法极为优雅：**保留 HC 的多路残差流和可学习连接，但强制每层的混合矩阵落在"双随机矩阵流形"上**。

**双随机矩阵**：行和、列和都等于 1 的非负矩阵。它有两个关键代数性质：

- **闭包性**：两个双随机矩阵相乘还是双随机矩阵。所以无论堆多少层，整体信号传播的"总量守恒"不变；
- **Birkhoff-von Neumann 定理**：任何双随机矩阵都可以表示为**置换矩阵的凸组合**。直觉：用双随机矩阵混合特征 = 只做"平均 + 重排"，**既不放大也不缩小信号**。

**一句话直觉**：mHC 层间的信息流动像水流经一组管道——HC 的无约束矩阵是"管径随机变化的管道"（水量会涨落），mHC 的双随机矩阵是"总量守恒的分流器"（流出去的水永远等于流进来的水）。恒等映射的"保真"性质由此被数学地锁死。

**实现**：用 **Sinkhorn-Knopp 算法**（一个快速把任意非负矩阵迭代投影成双随机矩阵的经典算法）在每次前向时做投影，配合 Sigmoid 保证非负性。据报告，mHC 在基础设施级优化后**计算开销几乎可忽略**。

### 2.2.3 与已知知识的连接

- 与 **W1 Pre-LN** 的呼应：Pre-LN 是通过"归一化位置"换训练稳定，mHC 是通过"残差结构约束"换训练稳定——同一个问题（深层信号控制）在不同时代的两种解法，前者已经是标准件，后者正在成为新标准件；
- 与 **Kimi AttnRes**（Part 4）的对比：两家都动了残差连接，但方向相反——mHC 做**减法**（约束连接空间，牺牲表达自由度换稳定性），AttnRes 做**加法**（让层间连接变成注意力，扩大表达自由度）。这个对比是本周最有意思的思考题之一，我们在 4.5 节展开。

## 2.3 CSA + HCA：序列维的混合压缩

### 2.3.1 问题：MLA 压了宽度，没压条数

回顾 MLA 的压缩逻辑（详见专题讲义）：MLA 把每个 token 的 KV 压缩成低维潜向量 $c^{KV}$（512 维替代 16384×2 维），**压的是每条 KV 的"宽度"**。但 KV 的**条数**——即序列长度 $n$——一点没变。

在 128K 时代这不是问题；到了 1M 时代：

- KV Cache 总量 = 条数 × 宽度。MLA 把宽度压到 1/32，但 1M 条数本身就把总量拉回来了；
- 注意力计算 $O(n^2)$：MLA 对计算复杂度没有任何改善。

结论：**想做 1M，必须把 $n$ 也压下来**。这就是 V4 混合注意力的使命——用两种压缩强度的注意力交替布置，分别对付计算和显存。

### 2.3.2 CSA：压缩 + 稀疏（对付计算）

**CSA（Compressed Sparse Attention）** 两步走：

1. **块合并**：每 $m=4$ 个 token 的 KV 合并成 1 条压缩 KV → 序列长度从 $n$ 变 $n/4$。直觉：相邻 4 个 token 的注意力键值通常高度相关，合并近似无损；
2. **稀疏选择**：在合并后的 KV 上，每个 query 只挑 top-$k$ 条做点积（继承 V3.2 的 DSA/DeepSeek Sparse Attention 索引机制）。最终每个 query 只读 $k$ 条 KV。

效果：注意力计算从"每 query 读 $n$ 条"降到"每 query 读 $k$ 条"（$k \ll n$），**计算量对序列长度近似线性**。

代价：4 倍合并 + 稀疏选择是有损的——**精确 recall（逐字复述、精确检索类任务）会受损**。

### 2.3.3 HCA：重度压缩 + 稠密（对付显存）

**HCA（Heavily Compressed Attention）** 走另一个极端：

1. **重度块合并**：每 $m'=128$ 个 token 的 KV 合并成 1 条 → 序列长度从 $n$ 变 $n/128$；
2. **不做稀疏选择**：合并后的短序列上做**全量 dense attention**。

效果：KV Cache 直接缩小约 128 倍（这是"KV 降至 10%"的主力），显存问题解决。

代价：128 合一的压缩必然丢失细粒度信息——HCA 层"记得大意，记不住细节"。

### 2.3.4 混合编排：两种损失互补

V4 的做法是**让 CSA 和 HCA 在不同层上交替出现**：

- CSA 层负责"精读"（压缩少 + 稀疏挑选，保关键细节的精确 recall）；
- HCA 层负责"泛读"（重度压缩 + 稠密，保全局轮廓的低成本记忆）。

这与 Qwen 的 GDN+QSA 混合（Part 3）、人脑的"细节记忆 + 要旨记忆"双系统是同构的设计哲学：**单一注意力机制无法同时满足"长序列便宜"和"关键信息精确"两个约束，就用两种各偏一头的机制交替**。总账：1M 上下文下 FLOPs 27%、KV 10%，且在 MRCR 1M 等长上下文基准上保持竞争力（V4-Pro 83.5，Opus-4.6 为 92.9——见 `专题学习/DeepSeek-V4-Pro-Benchmark分析.md`）。

**与 Week 13 的呼应**：RULER 教我们区分"声称上下文长度 vs 有效上下文长度"。V4 的 CSA+HCA 就是架构侧对这道裂缝的正面回应——不仅声称 1M，还让 1M 下的计算与显存落到服务可承担的区间。评测侧（W13）与架构侧（本周）在此闭环。

## 2.4 训练栈工程：Muon、FP4 QAT、Anticipatory Routing

架构创新的"下半场"在训练栈——V4 报告里这些名字值得记住：

- **Muon 优化器**（部分替代 Adam）：对动量做**正交化**（Newton-Schultz 迭代把更新矩阵投影到正交基），使每个参数维度的更新步长均衡，避免 Adam 的各维度步长失衡。DeepSeek 与 Qwen 3.8（Part 3）都采用了 Muon——2026 年它正在成为大模型训练的事实新标准。深层网络 + MoE 的路由矩阵对优化器步长极其敏感，Muon 的均衡性恰好对症；
- **FP4 QAT**：权重精度从 FP8（V3 首创大规模工程化）进一步降到 4 bit，QAT（量化感知训练）在训练中模拟量化误差。精度每降一半，HBM 容量与带宽压力减半（见 W6 量化的推理收益）；QAT vs PTQ 的取舍见 W6——规模到了，训练时多花的成本换推理时全生命周期的节省；
- **Anticipatory Routing**：MoE 路由的"预期性"稳定化——超稀疏 MoE 的负载均衡是训练稳定性的核心难题（Kimi 用 Quantile Balancing 解同一道题，见 4.2），DeepSeek 的解法是让路由"预见"专家负载再分配；
- **SwiGLU Clamp**：对激活函数输出做幅度钳制——W2 讲过 SwiGLU 的门控可能产生大激活值，深层 + 低精度训练下这会直接引发数值溢出，clamp 是工程兜底。

**一个观察**：V4 的创新清单里，"训练稳定性工程"占了一半以上。1M 上下文 + 100+ 层 + FP4 + 超稀疏 MoE，四个难度放大器叠加，每引入一个都要配一套稳定性机制——**架构创新的真实成本在配套工程，这是读技术报告时最容易低估的部分**。

## 2.5 V4.1 Flash：非对称编解码

V4.1 系列（Flash 版 2026-09-10 发布）引入了一个教科书级的新设计：**Causal-Encoder-Decoder（因果编码器-解码器）**。

**关键参数**：总参数 552B，**输入侧每 token 激活约 8B，输出侧每 token 激活约 16B**——两侧激活量差一倍。

**为什么这样设计？** 回顾 W6 的推理不对称性：

| 阶段 | 计算特征 | 瓶颈 | 最优架构倾向 |
|------|---------|------|-------------|
| Prefill（处理输入） | 计算密集（并行处理全部输入 token） | FLOPs | **小而快的模型**（算得快） |
| Decode（生成输出） | 访存密集（逐 token 增量生成） | 显存带宽 | **KV Cache 小、容量大的模型** |

旧架构（Decoder-only）两侧共享同一套参数与激活量，只能在"prefill 友好"与"decode 友好"之间折中。V4.1 的做法：**给编码侧配轻激活（8B，吞吐优先），给解码侧配重激活（16B，质量与缓存效率优先）**——把 W6 理论课讲的"不对称"直接烧进架构。

这与 W6 讲的 Chunked Prefilling、W8 的 Qwen3.5-Omni Chunked Prefilling 是同一主题的三个层次：调度层优化 → 编码器输出策略 → **模型结构本身的非对称化**。

**命名提示**：注意"Causal"前缀——编码器仍用因果掩码（非传统 NMT 的双向编码器），保留自回归预训练范式的兼容性，这是能复用海量 next-token-prediction 数据的关键工程决策。

## 2.6 性能画像与定位

基于 `专题学习/DeepSeek-V4-Pro-Benchmark分析.md`（2026-04 benchmark 数据）的画像：

- **编程：全场最强**（LiveCodeBench 93.5、Codeforces Rating 3206 均第一）——可量化、有明确答案的工程类任务上表现卓越，DeepSeek 一贯风格；
- **数学：一线水平**（IMOAnswerBench 89.8，第二）；
- **前沿推理与事实知识：明显短板**（Apex 38.3 vs Gemini-3.1-Pro 60.9；SimpleQA-Verified 57.9 vs 75.6）——超出训练分布的极端推理与幻觉抑制上，与第一梯队有差距；
- **长上下文：中等偏强**（MRCR 1M 83.5，第二）；
- **Agent：中规中矩**（SWE Verified 80.6 并列第一，但 Terminal-Bench 67.9 第四）。

**读法**（用 W12-W13 的评测素养）：技术报告的效率叙事（FLOPs 27%/KV 10%）是可信的工程事实；能力画像则要用"分数是系统属性"的眼光看——V4 的 profile 与其架构取向（工程效率极致）高度自洽：**架构决定性格**。

## 2.7 DeepSeek-OCR：视觉压缩的跨模态延伸

V4 家族同期还有 DeepSeek-OCR 值得一提：用 LLM 风格的架构（而非 CLIP 式对比学习编码器）处理视觉 token，通过**因果注意力掩码让视觉 token 根据语义动态重排序**，把"视觉压缩处理长文本"作为多模态入口——思想上与 CSA/HCA 的"压缩保语义"一脉相承。此方向与你的实战方向（Qwen-VL）相关，值得留意其后续演进。

---

# Part 3: Qwen3.8-Flash-Next 解析

## 3.1 定位：Qwen4 架构的公开预演

Qwen3.8-Flash-Next（2026-08-26，开源权重 preview）官方定位是**Qwen4 架构预览**：主模型 125B 参数 + 51B N-gram Embedding + ~4B MTP 模块，磁盘总量约 180B，**每 token 激活仅 6B**（激活比约 1/21——超稀疏 MoE）。原生上下文 262,144 token；正式版 Qwen3.8-Flash 默认 1M 上下文 + 官方内置工具。

Qwen 官方架构论文（arXiv 2608.30320，*On the Design of Qwen3.8-Next Architecture*）的三个主题：评测（Evaluation）、效率（Efficiency）、训练稳定性（Training Stability）——与 DeepSeek V4 的议程高度重叠，再次印证收敛。

## 3.2 五层解耦：一张参数的分类账

Qwen3.8-Flash-Next 最反直觉的设计是**参数的角色分离**。传统上我们说"一个 125B 模型"，隐含假设所有参数是同质的；Qwen3.8 把它们拆成五层，各自遵守不同的物理规律：

| 层 | 参数量 | 每次推理是否激活 | 存放位置 | 本质角色 |
|----|--------|----------------|---------|---------|
| **主模型容量** | 125B（MoE，大专家池 + 少量路由 + 1 共享专家） | 每 token 激活 6B | GPU 显存 | 可计算的推理能力 |
| **N-gram Embedding** | 51B | **不激活，纯查表** | **Host RAM（CPU 内存）** | 显式记忆 |
| **MTP 模块** | ~4B | 推测解码时使用 | GPU 显存 | 投机解码加速器 |
| **多模态编码** | （随输入） | 有输入时激活 | GPU 显存 | 视觉/音频入口 |
| **残差流扩展（GR）** | （含在主干） | 激活 | GPU 显存 | 深度维信息通路 |

**为什么这是深刻的设计**：它承认了"知识 ≠ 计算"。W9 记忆系统讲过参数化记忆（权重里的隐式知识）与外部记忆（RAG、文件系统）的光谱——51B N-gram Embedding 恰好落在光谱中段：**它是一块不参与梯度计算（推理时纯查表）、可以放在便宜介质上的"记忆硬盘"**：

- 参数化知识（MoE 权重）：计算获得，灵活但昂贵（必须过 GPU 矩阵乘）；
- **N-gram 查表知识：零计算获得，死板但近乎免费**（host RAM 的价格/容量比 GPU 显存低两个数量级，配合预取管线几乎不增加延迟）；
- 外部知识（RAG）：检索获得，最新鲜但需要额外系统。

N-gram 表存的是文本片段的共现统计（衔接 W9 的"程序性/词法记忆"概念）——对语言建模这种大量依赖局部词法模式（搭配、固定表达、专名）的任务，把这部分"死记硬背"外包给查表，主干 MoE 就能专注于真正需要推理的知识。**这是"把记忆从计算中剥离"的第一次架构级落地**，其意义可能超过本周任何单项注意力创新。

## 3.3 GDN + QSA 3:1 混合注意力

Qwen3.8 的注意力层按 **3:1** 交替：每 4 层中 3 层 GDN、1 层 QSA。这条路线在 W8 讲 Qwen3.5-Omni 时已经铺过理论地基，此处从架构设计视角加深：

**GDN（Gated DeltaNet，3/4 的层）——线性注意力，负责吞吐**：
- 用固定大小的循环状态替代 KV Cache：每来一个 token，状态做一次"门控 + delta 更新"（选择性写入/覆盖/遗忘），复杂度 $O(n)$、状态大小恒定；
- 天然适合**流式处理**与超长序列的吞吐（这是 1M 上下文 prefill 7.6x 加速的主力）；
- 代价：精确 recall 弱（见 W8 的"效率 vs recall 权衡"）。

**QSA（Qwen Sparse Attention，1/4 的层）——稀疏检索，负责精确性**：
- 与 GDN 不同，QSA 是**在标准注意力上做"先压缩索引、再稀疏计算"**：一个轻量的 Indexer 网络先把上下文组织成 micro-block（微块），并为每个块打重要性分；每个 query 只对高分的少量块做完整注意力；
- 与 DeepSeek CSA 的"块合并 + top-k"是同构思想（不约而同的收敛），区别在 Qwen 的 Indexer 是学习的压缩式打分器，DSA 是索引选择；
- MTP 模块也用 QSA，且**投机解码的多个候选步复用同一批 QSA 索引**——加速器与检索机制共享，工程上很漂亮。

**3:1 配比的含义**：大部分层走 $O(n)$ 快通道，少数层做精确检索"纠偏"。这与 DeepSeek 的 CSA/HCA 交替、人脑的 System1/System2 是同一设计模式的三次独立发现：**混合不是因为技术不成熟，而是"效率与精确 recall 不可兼得"是任务本质**。

## 3.4 Gated Residual 与训练稳定性

Qwen3.8 的每个子层读写都经过 **GR（Gated Residual）**：残差流被拓宽，且每个子层读取残差流时经过一个**逐元素门控**——网络可以学习"屏蔽掉残差流中与自己无关的通道"，写入时同样有门控。

与 mHC（2.2）对照着读最有意思：GR 和 mHC 都在解决"多路/宽残差流的信号管理"，但：
- **mHC 用硬约束**（双随机矩阵，数学保证总量守恒）；
- **GR 用软门控**（学习出来的 elementwise 掩码，灵活性最大但无数学保证）。

加上 Qwen3.8 也换用了 **Muon 优化器**（与 DeepSeek 同款），训练稳定性工具箱在三家里开始出现明显的交叉复用——**Muon 正在成为 2026 年大模型训练的事实标准**，这个趋势对做训练工程的人（包括你的实战项目）是直接相关的信号。

## 3.5 效率数字解读

官方报告的 1M token 场景数字（对照 Qwen3.7-Plus）：

- Prefill **7.6x** 加速：GDN 线性层扛住大部分 token 的 $O(n)$ 处理 + QSA 稀疏索引；
- Decode **4.9x** 加速：固定状态无 KV Cache 膨胀 + N-gram 查表不占 GPU 带宽；
- 稳态吞吐（带前缀缓存）**8.6x**：前缀缓存（见 W15 将讲的 Prefix Caching）与查表记忆的叠加收益。

**用 W6 的推理工程眼光读这些数字**：加速倍率都来自"减少访存 + 减少计算"两个杠杆的同时压榨；且注意"带缓存"与"不带缓存"的口径差异——W12/W13 反复训练过的"分数是测量系统的属性"，对效率数字同样成立。

---

# Part 4: Kimi K3/K2.8 解析

## 4.1 定位与产品策略

Kimi K3（2026-07-16 API 发布，2026-07-27 开源权重 + 47 页技术报告）：2.8T 总参数（当前最大开源权重模型）、原生视觉、1M token 上下文（较 K2 的 256k 扩大 4 倍），能力咬住闭源第一梯队，开源第一。

K2.8 Preview（2026-09-11）：**闭源**的中间层多模态模型，定位在 K2.7 Code 与旗舰 K3 之间，性能接近 K3，API ID 保持 `kimi-for-coding` 不变（客户端零改动）。

**产品策略的读法**（Phase 5 的商业视角）：K3 开源打生态位与口碑，K2.8 闭源收编企业付费场景——开源旗舰 + 闭源中间层的"高低搭配"是 2026 年国产厂商的典型打法（对比 DeepSeek 全开源路线、Qwen 的 open-weight preview→正式版节奏）。架构学习的素材主要来自 K3 技术报告。

## 4.2 超稀疏 MoE：896 选 16 与 LatentMoE

K3 的 FFN 侧配置：**896 个专家，每 token 激活 16 个（Top-16），稀疏比 56:1**，每 token 激活约 104B（约占总参 1/27）。

对比 W2 的 MoE 谱系表：Qwen2-57B 是 Top-8（激活比 ~1/7），DeepSeek-V3 是 256 选 8（Sigmoid 独立打分），K3 把稀疏度再推一个数量级。**稀疏度越高，"知识容量 vs 单 token 计算"的解耦越极致**（2.8T 容量配 104B 计算），但负载均衡的难度也非线性上升——896 个专家中任何一个被冷落/过热，训练就会崩溃或浪费。

K3 的两件新武器：

- **Stable LatentMoE**：路由不在"专家参数空间"直接做，而在一个学习到的**潜在空间**里组织路由（提高路由的泛化性与稳定性）；
- **Quantile Balancing**：按路由分数的**分位数**分配专家负载，替代传统辅助损失/启发式更新。回忆 W2：DeepSeek-V2 用"无辅助 Loss 的负载均衡"（更新偏置项）解决 Top-k 竞争；Quantile Balancing 是同一条"去辅助损失"路线的后续——辅助损失与主损失的梯度冲突问题（W2 已讲）在 56:1 稀疏度下更尖锐，必须用不进梯度的机制解。

报告称 K3 相比 K2 的**整体扩展效率提升约 2.5 倍**（同等算力换更多能力）——超稀疏 MoE + 架构升级的共同贡献。

## 4.3 KDA：从标量遗忘到逐维遗忘

K3 的注意力主干是 **KDA（Kimi Delta Attention）+ MLA 混合**（来自 Kimi Linear 研究线，2025-10 起步）。KDA 是对 Gated DeltaNet（W8、Part 3 已铺垫）的一次精确改进，值得用数学写清楚，因为它体现了"识别瓶颈→最小改动"的品味。

**GDN 的遗忘机制**：循环状态 $S_t$ 的更新形如

$$S_t = \alpha_t S_{t-1} + \beta_t \cdot (\text{新信息})$$

其中 $\alpha_t \in (0,1)$ 是一个**标量**衰减因子——状态的所有维度以同一个速率遗忘。

**问题**：记忆的不同通道需要不同的遗忘速率。比如处理一段代码时，"变量名"通道需要保持（后面还要用），"空格排版"通道可以立刻遗忘——**标量衰减是维度级的钝刀**。

**KDA 的改进**：把标量换成向量，展开为**对角矩阵** $\text{diag}(\alpha_t) \in \mathbb{R}^{d \times d}$：

$$S_t = \text{diag}(\alpha_t)\, S_{t-1} + \Delta_t$$

每个状态维度获得**独立的遗忘率**——通道级的精细控制。代价是对角矩阵乘法比标量乘贵一点，但 Kimi 的工程优化（关联 chunk 级并行）把它做到了可扩展（2.8T 规模落地验证）。

**一句话总结 KDA：GDN 让模型"能遗忘"，KDA 让模型"每个记忆通道按需遗忘"**——这是把"选择性记忆"从层级别下放到维度级别的精度提升。

## 4.4 AttnRes：深度维的选择性检索

K3 对残差连接的解法（AttnRes, Attention Residuals）与 mHC 方向截然相反，是本周最重要的对比素材。

**旧范式（标准残差）**：

$$h_L = h_0 + F_1(h_0) + F_2(\cdot) + \cdots$$

每一层的输出以**相等的权重**累加进残差流。问题（Kimi 的诊断）：随深度增长，每层的贡献被**稀释**——第 100 层的输出与前面 99 层的输出等权求和，模型无法"重点参考"某几个关键层。

**AttnRes 的设计**：把层间信息传递变成**注意力**——每层（实际是每个 Block，见下）的输出作为 key/value，一个可学习的**伪查询**（pseudo-query）向量做 softmax，对前序所有层的表征加权检索：

$$h_{\text{out}} = \sum_{l} \text{softmax}(q_{\text{pseudo}} \cdot k_l)\, v_l$$

**深度的注意力化**：从此每一层可以**动态、按输入内容**地决定"重点继承哪些前层的表征"——固定求和是"所有前辈等权发言"，AttnRes 是"轮到谁发言由内容决定"。

**两个关键工程细节**：

1. **伪查询零初始化**：训练初期 softmax 权重均匀 → 等价于标准等权平均（与 W3 LoRA 的 B 零初始化是同一思想：**新机制起步时必须退化为旧机制，保证训练不倒退**）；
2. **Block 变体**：不对每层做注意力（开销 O(L²)），而是把层分组成 N 个 Block，Block 间做 AttnRes、Block 内标准残差——把深度注意力的开销控制住（这是 2.8T 规模落地的必要工程化）。

## 4.5 KDA vs AttnRes vs mHC：三个坐标轴

Kimi 官方的一句话总结非常精准：**"KDA 是时间维的选择性遗忘，AttnRes 是深度维的选择性检索"**。加上 DeepSeek 的 mHC，三家对信息流的手术可以放进一个二维坐标：

| 机制 | 维度 | 操作 | 约束哲学 |
|------|------|------|---------|
| **KDA** | 时间维（序列轴） | 逐通道选择性遗忘 | 软机制（学习门控，无硬保证） |
| **AttnRes** | 深度维（层轴） | 选择性检索前层 | 软机制（softmax 权重，零初始化保护） |
| **mHC** | 深度维（层轴） | 总量守恒的混合 | **硬约束**（双随机流形，数学保证） |

**AttnRes vs mHC 的哲学对比**（建议作为深度思考题）：

- 两者都承认"固定残差求和"已经过时；
- mHC 走**约束路线**：先保稳定（范数有界），表达能力在约束流形内优化——**悲观主义工程**（假设自由度会失控）；
- AttnRes 走**表达路线**：先要灵活（内容相关的层间路由），稳定性靠初始化与 Block 化控制——**乐观主义工程**（假设自由度能被学习驯服）；
- 有趣的是两条路线在 2026 年同时落地到万亿级模型，且各自报告了正收益。**深度维设计空间还远没有收敛**——这是与"注意力层 MHA→GQA 已收敛 10 年"形成鲜明对比的开放前沿。

## 4.6 评测侧速览

K3 的公开评测要点（用 W12/W13 的防御性阅读姿势）：代码/Agent 能力是主打（对齐 K2.7 Code 的产品线惯性），通用推理第一梯队；具体分数建议跟踪 Arena 与 SWE-Bench Verified 的（模型 × harness）条目而非厂商自报数字（W13 的 Agent 分数公式直接适用：K3 的 1M 上下文 + Agent 能力分数，必须先问 harness 与评测环境版本）。

---

# Part 5: 三家横向综合

## 5.1 收敛主题总结

把三家的创新并排放，五个收敛主题清晰可见：

| 主题 | DeepSeek V4 | Qwen3.8 | Kimi K3 | 共同动机 |
|------|-------------|---------|---------|---------|
| **序列维压缩** | CSA+HCA 交替 | GDN+QSA 3:1 | KDA+MLA 混合 | 1M 上下文的计算/显存可行性 |
| **深度维重构** | mHC（硬约束） | Gated Residual（软门控） | AttnRes（层间注意力） | 100+ 层的信号管理与训练稳定 |
| **超稀疏 MoE** | DeepSeekMoE 细粒度 | 大池+少路由+共享专家 | 896选16 + LatentMoE | 知识容量与单 token 计算解耦 |
| **显式记忆回归** | （无） | 51B N-gram 查表 | （无） | 记忆与计算的成本分离 |
| **训练稳定性工程** | Muon/FP4/AR/SwiGLU-Clamp | Muon/GR | Quantile Balancing | 更深+更稀疏+更低精度的叠加风险 |

**收敛的原因不是抄作业**（三家技术报告的引用关系显示是并行独立探索），而是**外部约束相同**：1M 上下文成为标配、Agent 时代的推理成本压力、训练硬件的性价比极限。同一道考题，三份接近满分的答案——这比任何单项创新都更能说明**这些主题是下一代的"标准件"候选**。

## 5.2 分歧点：路线选择的性格差异

收敛中有分歧，且分歧与各家"性格"（产品定位/资源禀赋）高度相关：

| 分歧点 | DeepSeek | Qwen | Kimi |
|--------|----------|------|------|
| 长上下文主力机制 | 压缩+稀疏（在注意力框架内改造） | 线性注意力为主（跳出 KV Cache 范式） | 线性注意力精细化（GDN 微调） |
| 残差哲学 | 数学约束（稳） | 门控（中） | 注意力（活） |
| 记忆外置 | 无 | **51B 查表**（独此一家） | 无 |
| 非对称设计 | **编解码两侧激活不对称**（独此一家） | 无 | 无 |
| 稀疏度 | 中等 | 中等 | **56:1 最激进** |

**读法**：DeepSeek 的两件独家武器（非对称编解码、极致 KV 压缩）都指向"推理服务成本"，与 V4 主线一致；Qwen 的独家武器（N-gram 记忆外置）指向"容量成本分离"，与其多模态 + 全模态产品矩阵的大容量需求一致；Kimi 的激进稀疏度指向"最大知识容量"，与其开源旗舰打口碑的定位一致。**架构是产品战略的函数**——这句话可以作为 Phase 7 的主题句。

## 5.3 对"Transformer 后时代"的判断

本周证据支持的判断（供批判性讨论，非定论）：

1. **Transformer 不是被替代，而是被"逐件拆修"**：注意力的 $O(n^2)$、残差的固定求和、FFN 的稠密计算、Adam、BF16——五个组件全部在 2026 年被重写，但"堆叠的序列处理网络"这个骨架仍在。类比内燃机到电动车的过渡：动力系统换了，"车"的概念没换；
2. **混合架构是终局而非过渡**："线性层扛吞吐 + 稀疏/全注意力层保 recall"在三家全部落地，说明这不是技术不成熟的妥协，而是任务本质决定的双系统需求（人脑的 System1/System2 同构）；
3. **下一个悬念在深度维**：注意力序列维的答案已收敛（混合），但 mHC/AttnRes/GR 三条残差路线还在分叉——关注 2026-2027 年哪家先给出"深度维设计空间收敛"的证据；
4. **记忆的形态光谱正在被工程化**：从纯参数化（旧）→ RAG 外挂（W9）→ 架构内查表，三档成本/灵活性不同的记忆形态将共存——Qwen 的 N-gram 表是第一条被万亿级模型验证的中间路线。

---

# Part 6: 前沿发展速览

**各家最新动态**（截至 2026-09）：

- **DeepSeek**：V4.1 Flash（2026-09-10）以 552B/8B-16B 非对称编解码成为新架构系列最小成员；V4-Flash 用 32T、V4-Pro 用 33T token 预训练；mHC 论文（arXiv 2512.24880）被视为 2026 开年最有影响力的架构论文之一；MIT 协议 + 完整技术报告的开源姿态延续；
- **Qwen**：3.8-Flash-Next（2026-08-26）为 Qwen4 预演；正式版 3.8-Flash 默认 1M 上下文 + 官方内置工具；架构论文（arXiv 2608.30320）公开了评测/效率/稳定性三个维度的完整实验，是学习"架构研究方法论"的好材料；vLLM/SGLang day-0 支持其开源权重；
- **Kimi**：K3 开源（2026-07-27）+ 训练与算子基础设施同步开放（对研究者极有价值）；K2.8 Preview（2026-09-11）闭源中间层上线 Kimi Code/Work，可调思考档位；AttnRes 论文获马斯克与 Karpathy 公开点赞，学术影响力破圈；
- **横向观察**：Muon 优化器在三家的交叉采用是最强的"事实标准"信号；1M 上下文已从旗舰特性降级为"标配"；每家都在"开源旗舰打口碑 + 闭源/服务版本收收入"的商业结构上做文章。

**值得关注的方向**（衔接 W15）：Prefix Caching 与混合注意力的叠加收益（Qwen 吞吐 8.6x 的构成）、MTP 与稀疏注意力的索引复用（投机解码的架构协同）、非对称编解码对 Agent 场景（长 prompt + 短输出的典型负载）的意义。

---

## 💡 实践启示

1. **对微调实战（Qwen-VL + CameraBench）的直接意义**：本周三家全部在动"注意力与残差"，而 LoRA 微调时 target_modules 的选择（W3 的 q/k/v/o/gate/up/down）在新架构下将发生变化——GDN 层的 delta 更新矩阵、QSA 的 Indexer、GR 的门控都可能成为新的高效微调目标。做实战时先看一眼所用 backbone 的架构再定 target_modules；
2. **推理部署选型**：1M 上下文需求下，backbone 的注意力机制（CSA/HCA、GDN+QSA、KDA）直接决定服务成本量级——选型时"每百万 token 的推理成本"应成为与"基准分数"并列的第一指标（W13 的评测素养直接落地）；
3. **架构阅读姿势**：读技术报告先找"主线命题"（V4 的 1M 日用化、Qwen 的容量解耦、K3 的信息流动），所有单项创新都能挂回主线——**没有主线的创新清单通常意味着工程拼凑，有主线的才值得精读**；
4. **训练工程的迁移信号**：Muon、量化感知训练、路由稳定性机制正在标准化——这些不是"万亿俱乐部专属"，中小规模训练（包括实战项目的 LoRA/full FT）也在快速吸收同款思想（如 QAT 思想的 LoRA 变体）。

## ✅ 检查点

- [ ] 能画出主流模型 (GPT-3/LLaMA/Qwen/DeepSeek) 的技术选择对比表
- [ ] 理解从 GPT-3 到 LLaMA 的关键架构变化及其动机
- [ ] 理解演进"两股绳"（能力 vs 效率）与 2025-2026 五条战线
- [ ] 理解 mHC 如何用双随机矩阵流形约束解决深层网络训练不稳定性（Birkhoff 定理直觉）
- [ ] 理解 CSA+HCA 如何分别对付计算与显存，及"MLA 压宽度不压条数"的 1M 瓶颈
- [ ] 理解 V4.1 非对称编解码与 W6 prefill/decode 不对称性的同源关系
- [ ] 理解 Qwen3.8 五层解耦，特别是 51B N-gram 查表记忆的"记忆与计算成本分离"意义
- [ ] 理解 GDN+QSA 3:1 混合与 DeepSeek CSA/HCA 的同构性（混合是终局）
- [ ] 理解 KDA 的对角遗忘与 AttnRes 的深度注意力，及其"软机制 vs mHC 硬约束"的哲学对比
- [ ] 能说出三家收敛的五大主题与各自的独家武器，并解释"架构是产品战略的函数"

---

## 📎 附录

### A.1 双随机矩阵与 mHC 的数学细节

> 关联正文：**2.2 节 mHC**

**定义**：矩阵 $M \in \mathbb{R}^{n \times n}$ 是双随机的，当且仅当 $M_{ij} \geq 0$ 且 $\sum_j M_{ij} = 1$（行随机）、$\sum_i M_{ij} = 1$（列随机）。

**Birkhoff-von Neumann 定理**：双随机矩阵集合（Birkhoff 多胞形）恰好是全体 $n!$ 个置换矩阵的凸包：

$$M = \sum_{\pi \in S_n} \lambda_\pi P_\pi, \quad \lambda_\pi \geq 0, \; \sum_\pi \lambda_\pi = 1$$

**信号传播的守恒证明（简版）**：对任意特征向量 $x$，$\|Mx\|_1$ 的守恒由行随机性保证"总量不变"，列随机性保证"重分配不偏斜"。层层复合（乘积仍双随机）后，无论多少层，信号总能量既不指数放大也不衰减——这正是 ResNet 恒等映射性质的"可学习加强版"（恒等映射只是 $M = I$ 这一个点，mHC 把可用空间扩展到整个多胞形）。

**Sinkhorn-Knopp 投影**：给定任意非负矩阵 $A$，交替执行"行归一化 / 列归一化"迭代，收敛到双随机矩阵 $\Pi(A)$。复杂度低、可微（mHC 训练时梯度可穿过投影），这就是"流形约束"的落地机制。

### A.2 CSA/HCA 分块压缩的代价分析

> 关联正文：**2.3 节 CSA+HCA**

以 1M token、head 维度 128 为例的粗账：

| 方案 | KV 条数 | 每 query 计算量 | 精确 recall 风险 |
|------|---------|----------------|-----------------|
| MLA（V3 基线） | $n = 1\text{M}$ | $O(n)$ 全读 | 无（但贵） |
| CSA（m=4 + top-k） | $n/4$，读 $k$ 条 | $O(k)$ | 中（4 合 1 有损 + 选择漏检） |
| HCA（m'=128） | $n/128 \approx 8\text{K}$ | $O(n/128)$ 全读 | 高（128 合 1，细节必损） |

混合交替的意义：HCA 层提供"廉价的全局轮廓"，CSA 层对关键区域做"精读"，两者信息在残差流中汇合——单层各有损，组合近似无损。注意这与 W13 RULER 的多跳任务（跨段落信息聚合）直接相关：压缩注意力的 recall 损失在 RULER 类任务上最先暴露，这是读 V4 长上下文分数时的背景知识。

### A.3 KDA 遗忘机制的推导与 GDN 对照

> 关联正文：**4.3 节 KDA**

**GDN（标量遗忘）**：

$$S_t = \alpha_t S_{t-1} + \beta_t u_t v_t^\top, \quad \alpha_t \in (0,1) \text{ 标量}$$

所有状态通道同速率衰减。等价于"整个记忆一个保质期"。

**KDA（对角遗忘）**：

$$S_t = \text{diag}(\alpha_t) S_{t-1} + \Delta_t, \quad \alpha_t \in (0,1)^d \text{ 向量}$$

第 $i$ 个通道的遗忘率由 $\alpha_{t,i}$ 独立控制。等价于"每个记忆通道自己的保质期"。例：通道 3 存"当前函数的返回类型"（长保质期，$\alpha \to 1$），通道 7 存"上一个标点"（短保质期，$\alpha \to 0$）。

**为什么是对的改进方向**：注意力 softmax 的每通道 logit 变化速率本就不同（W1 讲过的各维度语义分化），统一遗忘率与维度语义分化天然冲突；KDA 把遗忘的自由度对齐到语义自由度。代价：对角矩阵乘多 $d$ 次乘法/步，工程上靠 chunk 并行摊销。

### A.4 三家规格速查表

> 关联正文：**Part 2/3/4**

| 维度 | DeepSeek V4.1 Flash | Qwen3.8-Flash-Next | Kimi K3 |
|------|---------------------|--------------------|---------| 
| 发布 | 2026-09-10 | 2026-08-26（Qwen4 预演） | 2026-07-16 API / 07-27 开源 |
| 总参数 | 552B | 180B（125B 主干 + 51B N-gram + 4B MTP） | 2.8T |
| 每 token 激活 | 输入侧 ~8B / 输出侧 ~16B（非对称） | 6B | ~104B |
| 注意力 | CSA+HCA 混合（MLA 系谱） | GDN+QSA 3:1（含 MLA 全注意力层） | KDA+MLA 混合 |
| 残差 | mHC（双随机约束） | Gated Residual | AttnRes（Block 级层间注意力） |
| MoE | DeepSeekMoE + Anticipatory Routing | 大池超稀疏 + 共享专家 | 896选16（56:1）+ LatentMoE + Quantile Balancing |
| 上下文 | 1M（原生） | 262K 原生（正式版 1M） | 1M |
| 优化器/精度 | Muon + FP4 QAT | Muon | -（报告中未完整披露） |
| 独家武器 | 非对称编解码 / KV 压到 10% | N-gram 查表记忆外置 | 最激进稀疏度 / 记忆-检索双改 |
| 开源 | 是（MIT） | 是（open-weight） | 是（旗舰）/ K2.8 闭源 |

*表中数字来自各技术报告/官方页（2026-09 检索），以官方最新披露为准。*

---

## 参考与延伸

- DeepSeek-V4 技术报告（2026-04-24，HuggingFace PDF）；mHC: arXiv 2512.24880
- Qwen3.8-Flash-Next: arXiv 2608.30320（*On the Design of Qwen3.8-Next Architecture*）；Qwen 官方博客
- Kimi K3 技术报告（GitHub: MoonshotAI/Kimi-K3）；Kimi Linear（KDA 出处）；AttnRes 论文
- 本项目内：`专题学习/MLA专题讲义.md`（MLA 完整数学）、`专题学习/DeepSeek-V4-Pro-Benchmark分析.md`（V4 能力画像）、W1/W2（标准件原理）、W6（推理不对称性）、W8（GDN 理论地基）、W9（记忆系统光谱）、W13（长上下文评测侧）

> **下讲预告**：Week 15 将继续 Qwen 全家桶（VL/Omni 与 M-RoPE 细节）、面向 Agent 的架构优化（Prefix Caching/并行工具调用/结构化输出），以及持续学习（Nested Learning/HOPE）与模型融合（Task Arithmetic/TIES/DARE）——其中模型融合与本周的"权重空间"讨论（mHC 的层间混合、AttnRes 的表征检索）将形成"训练时融合 vs 训练后融合"的完整对照。
